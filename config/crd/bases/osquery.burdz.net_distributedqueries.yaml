---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: distributedqueries.osquery.burdz.net
spec:
  group: osquery.burdz.net
  names:
    kind: DistributedQuery
    listKind: DistributedQueryList
    plural: distributedqueries
    shortNames:
    - oqdq
    - dq
    singular: distributedquery
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.completedNodes
      name: Completed
      type: string
    - jsonPath: .status.targetNodes
      name: Target
      type: string
    - jsonPath: .status.totalRows
      name: Rows
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DistributedQuery is the Schema for ad-hoc queries across nodes
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DistributedQuerySpec defines the desired state of DistributedQuery
            properties:
              agentRef:
                description: AgentRef references which OsqueryAgent to use
                type: string
              cardinality:
                default: 1000
                description: Cardinality limits the number of rows per node
                maximum: 10000
                minimum: 1
                type: integer
              nodeSelector:
                additionalProperties:
                  type: string
                description: NodeSelector targets specific nodes (empty = all nodes)
                type: object
              query:
                description: Query is the SQL query to execute
                type: string
              timeout:
                default: 60s
                description: Timeout for query execution per node
                type: string
              ttl:
                default: 1h
                description: TTL auto-deletes this query after completion
                type: string
            required:
            - query
            type: object
          status:
            description: DistributedQueryStatus defines the observed state of DistributedQuery
            properties:
              completedNodes:
                description: CompletedNodes is the number of nodes that have responded
                type: integer
              completionTime:
                description: CompletionTime when all nodes responded (or timed out)
                format: date-time
                type: string
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              failedNodes:
                description: FailedNodes is the number of nodes that failed to respond
                type: integer
              nodeResults:
                description: NodeResults contains per-node result summaries
                items:
                  description: NodeQueryResult contains results from a single node
                  properties:
                    completedAt:
                      description: CompletedAt when this node responded
                      format: date-time
                      type: string
                    error:
                      description: Error message if the query failed
                      type: string
                    nodeName:
                      description: NodeName is the name of the node
                      type: string
                    rowCount:
                      description: RowCount returned by this node
                      type: integer
                    rows:
                      description: |-
                        Rows contains the actual query results (if small enough)
                        For large results, these are stored in QueryResult CRs
                      items:
                        additionalProperties:
                          type: string
                        type: object
                      type: array
                    status:
                      description: 'Status of the query on this node: Pending, Completed,
                        Failed, TimedOut'
                      type: string
                  required:
                  - nodeName
                  type: object
                type: array
              phase:
                description: 'Phase of the distributed query: Pending, Running, Completed,
                  Failed'
                enum:
                - Pending
                - Running
                - Completed
                - Failed
                - TimedOut
                type: string
              startTime:
                description: StartTime when the query was dispatched
                format: date-time
                type: string
              targetNodes:
                description: TargetNodes is the number of nodes targeted
                type: integer
              totalRows:
                description: TotalRows across all nodes
                type: integer
            type: object
        required:
        - metadata
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
