---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: osquerypacks.osquery.burdz.net
spec:
  group: osquery.burdz.net
  names:
    kind: OsqueryPack
    listKind: OsqueryPackList
    plural: osquerypacks
    shortNames:
    - oqp
    singular: osquerypack
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.queryCount
      name: Queries
      type: integer
    - jsonPath: .status.deployedToNodes
      name: Nodes
      type: integer
    - jsonPath: .spec.disabled
      name: Disabled
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OsqueryPack is the Schema for the osquerypacks API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OsqueryPackSpec defines the desired state of OsqueryPack
            properties:
              disabled:
                default: false
                description: Disabled allows temporarily disabling a pack
                type: boolean
              nodeSelector:
                additionalProperties:
                  type: string
                description: NodeSelector targets specific nodes for this pack
                type: object
              platform:
                default: linux
                description: Platform restricts this pack to specific platforms
                enum:
                - linux
                - darwin
                - windows
                type: string
              queries:
                description: Queries in this pack
                items:
                  description: PackQuery defines a single query within a pack
                  properties:
                    description:
                      description: Description of what this query does
                      type: string
                    disabled:
                      default: false
                      description: Disabled allows temporarily disabling a query
                      type: boolean
                    interval:
                      description: Interval in seconds between query executions
                      minimum: 10
                      type: integer
                    name:
                      description: Name of the query (must be unique within pack)
                      type: string
                    query:
                      description: Query is the SQL query to execute
                      type: string
                    severity:
                      default: info
                      description: Severity for results from this query
                      enum:
                      - info
                      - low
                      - medium
                      - high
                      - critical
                      type: string
                    snapshot:
                      default: false
                      description: Snapshot mode returns all results each time (vs
                        differential)
                      type: boolean
                  required:
                  - interval
                  - name
                  - query
                  type: object
                type: array
            required:
            - queries
            type: object
          status:
            description: OsqueryPackStatus defines the observed state of OsqueryPack
            properties:
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              deployedToNodes:
                description: DeployedToNodes lists nodes where this pack is active
                items:
                  type: string
                type: array
              lastUpdated:
                description: LastUpdated is when the pack was last reconciled
                format: date-time
                type: string
              queryCount:
                description: QueryCount is the number of active queries in this pack
                type: integer
            type: object
        required:
        - metadata
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
