---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: osqueryalerts.osquery.burdz.net
spec:
  group: osquery.burdz.net
  names:
    kind: OsqueryAlert
    listKind: OsqueryAlertList
    plural: osqueryalerts
    shortNames:
    - oqal
    - alert
    singular: osqueryalert
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.severity
      name: Severity
      type: string
    - jsonPath: .status.fireCount
      name: Fired
      type: integer
    - jsonPath: .status.lastFired
      name: Last Fired
      type: date
    - jsonPath: .spec.disabled
      name: Disabled
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OsqueryAlert defines alerting rules for query results
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OsqueryAlertSpec defines the desired state of OsqueryAlert
            properties:
              condition:
                description: Condition that triggers the alert
                properties:
                  fieldThreshold:
                    description: FieldThreshold fires when a numeric field exceeds
                      threshold
                    properties:
                      field:
                        description: Field name containing numeric value
                        type: string
                      operator:
                        description: 'Operator: gt, gte, lt, lte, eq'
                        enum:
                        - gt
                        - gte
                        - lt
                        - lte
                        - eq
                        type: string
                      value:
                        description: Value to compare against
                        format: int64
                        type: integer
                    required:
                    - field
                    - operator
                    - value
                    type: object
                  rowCount:
                    description: RowCount fires when result count meets threshold
                    properties:
                      operator:
                        description: 'Operator: gt, gte, lt, lte, eq'
                        enum:
                        - gt
                        - gte
                        - lt
                        - lte
                        - eq
                        type: string
                      value:
                        description: Value to compare against
                        type: integer
                    required:
                    - operator
                    - value
                    type: object
                  rowMatch:
                    description: RowMatch fires when any row matches all specified
                      field patterns
                    items:
                      description: FieldMatch defines a pattern to match against a
                        field
                      properties:
                        contains:
                          description: Contains substring match
                          type: string
                        equals:
                          description: Equals exact value match (mutually exclusive
                            with regex)
                          type: string
                        field:
                          description: Field name to match
                          type: string
                        notEquals:
                          description: NotEquals inverts the match
                          type: string
                        regex:
                          description: Regex pattern to match (mutually exclusive
                            with equals)
                          type: string
                      required:
                      - field
                      type: object
                    type: array
                  type:
                    description: 'Type of condition: rowMatch, rowCount, fieldThreshold'
                    enum:
                    - rowMatch
                    - rowCount
                    - fieldThreshold
                    - any
                    type: string
                required:
                - type
                type: object
              disabled:
                default: false
                description: Disabled allows temporarily disabling an alert
                type: boolean
              notify:
                description: Notify configures alert destinations
                properties:
                  kubernetes:
                    description: Kubernetes emits K8s Events and/or updates QueryResult
                      status
                    properties:
                      createEvent:
                        default: true
                        description: CreateEvent emits a K8s Event
                        type: boolean
                      eventType:
                        default: Warning
                        description: 'EventType: Normal or Warning'
                        enum:
                        - Normal
                        - Warning
                        type: string
                    type: object
                  slack:
                    description: Slack webhook notification
                    properties:
                      channel:
                        description: Channel to post to (overrides webhook default)
                        type: string
                      username:
                        description: Username for the bot
                        type: string
                      webhookSecretRef:
                        description: WebhookSecretRef references a secret containing
                          the webhook URL
                        properties:
                          name:
                            description: name is unique within a namespace to reference
                              a secret resource.
                            type: string
                          namespace:
                            description: namespace defines the space within which
                              the secret name must be unique.
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                    required:
                    - webhookSecretRef
                    type: object
                  webhook:
                    description: Webhook generic HTTP notification
                    properties:
                      headers:
                        additionalProperties:
                          type: string
                        description: Headers to include
                        type: object
                      secretRef:
                        description: SecretRef for auth headers
                        properties:
                          name:
                            description: name is unique within a namespace to reference
                              a secret resource.
                            type: string
                          namespace:
                            description: namespace defines the space within which
                              the secret name must be unique.
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                      url:
                        description: URL to POST to
                        type: string
                    required:
                    - url
                    type: object
                type: object
              querySelector:
                description: QuerySelector matches queries by name or labels
                properties:
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: MatchLabels selects queries by labels
                    type: object
                  packName:
                    description: PackName matches queries from a specific pack
                    type: string
                  queryName:
                    description: QueryName matches a specific query name
                    type: string
                type: object
              severity:
                default: medium
                description: Severity of this alert
                enum:
                - info
                - low
                - medium
                - high
                - critical
                type: string
              throttle:
                description: Throttle prevents alert spam
                properties:
                  groupBy:
                    description: GroupBy fields to throttle per unique combination
                    items:
                      type: string
                    type: array
                  maxAlerts:
                    default: 1
                    description: MaxAlerts per period (0 = no limit)
                    type: integer
                  period:
                    default: 15m
                    description: Period to throttle alerts
                    type: string
                type: object
            required:
            - condition
            - notify
            - querySelector
            type: object
          status:
            description: OsqueryAlertStatus defines the observed state of OsqueryAlert
            properties:
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              fireCount:
                description: FireCount total times this alert has fired
                format: int64
                type: integer
              lastEvaluated:
                description: LastEvaluated when the alert was last evaluated
                format: date-time
                type: string
              lastFired:
                description: LastFired when this alert last triggered
                format: date-time
                type: string
              throttledCount:
                description: ThrottledCount alerts suppressed by throttling
                format: int64
                type: integer
            type: object
        required:
        - metadata
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
